# `openapi-typescript@7` déclare un peer `typescript@^5` alors que le projet est en TypeScript 6 :
# sans cette ligne, un simple `npm install` échoue en ERESOLVE. Le générateur fonctionne bien
# avec TS 6 (client régénéré et typecheck vert) — le peer est seulement en retard.
legacy-peer-deps=true
